@model IEnumerable<WebApplication3.Models.Category>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CategoryName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Picture)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CategoryName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td class="pildigrupp" data-id="@item.CategoryID">

            <div>
                @*<form action="@Url.Action("ChangePicture", new { id = item.CategoryID })" method="post" enctype="multipart/form-data">*@
                    <img class="piltise vajutatavpilt" height="100" src="@Url.Action("Picture", new { id = item.CategoryID })" />
                    <input type="file" name="file" class="pildinupp" />
                    <button class="vahetapilt">Vaheta pilt</button>
                @*</form>*@
            </div>     
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CategoryID }) |
            @Html.ActionLink("Details", "Details", new { id=item.CategoryID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CategoryID })
        </td>
    </tr>
}

</table>
